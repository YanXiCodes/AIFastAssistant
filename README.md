# AI Fast Assistant

> 一个轻量级的 AI 浮窗助手，让你在工作中随时快速提问，不打断当前流程

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Platform](https://img.shields.io/badge/platform-Windows-lightgrey.svg)
![Electron](https://img.shields.io/badge/Electron-28.0-47848F.svg)

## 💡 为什么做这个？

在日常工作和编程中，我经常遇到这样的痛点：

- 正在专注写代码，突然想查个 Linux 命令，但不想打开浏览器或新建标签页
- 看文档时遇到不懂的概念，想快速问一下 AI，但切换窗口太麻烦
- 需要翻译一段文字，或者让 AI 帮忙 review 代码，但现有的 AI 工具都太"重"了
- 每次都要打开网页版 ChatGPT，等待加载，还要管理对话历史

我需要的是：**一个随时唤起、快速提问、用完即走的 AI 助手**。

于是我用 Electron + DeepSeek API 做了这个小工具。

## ✨ 核心特性

### 🚀 极速体验
- **全局快捷键唤起**：`Ctrl+Shift+Space` 随时呼出，`Esc` 快速关闭
- **流式响应**：实时显示 AI 回答，无需等待完整响应
- **轻量启动**：原生 HTML/CSS/JS，无重型框架，启动速度快
- **始终置顶**：浮窗始终在最前，不会被其他����遮挡

### 🎯 智能功能
- **选中文本快速查询**：选中文字后按快捷键，自动填充到输入框
- **快捷命令**：输入 `/` 触发命令菜单，快速切换 AI 模式
  - `/trans` - 翻译模式
  - `/code` - 代码审查
  - `/debug` - 调试助手
  - `/linux` - Linux 命令速查
  - `/sql` - SQL 助手
  - `/regex` - 正则表达式
  - `/git` - Git 助手
  - `/explain` - 简单解释
- **历史记录**：自动保存最近 10 条对话，快速重新提问
- **预设模板**：10+ 专业场景预设，一键切换 AI 角色

### 🎨 界面优化
- **代码高亮**：支持 180+ 编程语言语法高亮
- **Markdown 渲染**：完整支�� Markdown 格式，表格、代码块、引用等
- **透明度调节**：30%-100% 可调，多任务时可透视后面内容
- **智能滚动**：检测用户手动滚动，不会强制跳到底部
- **深色主题**：护眼的紫色渐变配色

### 💰 成本友好
- 使用 DeepSeek API，成本是 GPT-4 的 1/50
- 每次新对话，无历史负担，节省 token

## 📸 截图

> TODO: 添加应用截图

## 🚀 快速开始

### 安装

#### 方式 1：下载安装包（推荐）
1. 前往 [Releases](https://github.com/YanXiCodes/AeroFloat-/releases) 下载最新版本
2. 运行安装程序或解压便携版
3. 启动应用，配置 DeepSeek API Key

#### 方式 2：从源码运行
```bash
# 克隆仓库
git clone https://github.com/YanXiCodes/AeroFloat-.git
cd AeroFloat-

# 安装依赖
npm install

# 启动应用
npm start
```

### 配置

1. 获取 DeepSeek API Key：访问 [platform.deepseek.com](https://platform.deepseek.com)
2. 首次启动会提示输入 API Key
3. 也可以在设置中修改快捷键、透明度等

## 🎮 使用方法

### 基础操作
- `Ctrl+Shift+Space` - 唤起/隐藏窗口
- `Ctrl+Enter` - 发送消息
- `Esc` - 关闭窗口
- `Ctrl+V` - 粘贴图片（支持 OCR）

### 快捷命令
在输入框输入 `/` 即可触发命令菜单：
- 使用 `↑` `↓` 方向键选择
- `Enter` 确认选择
- `Esc` 取消

### 选中文本查询
1. 在任何地方选中文字
2. 按 `Ctrl+Shift+Space` 唤起窗口
3. 文字自动填充到输入框

### 历史记录
- 点击标题栏 📜 按钮查看历史
- 点击历史记录快速重新提问

## 🛠️ 技术栈

- **框架**：Electron 28
- **AI 服务**：DeepSeek API
- **配置存储**：electron-store
- **Markdown 渲染**：marked.js
- **代码高亮**：highlight.js
- **前端**：原生 HTML/CSS/JavaScript（无框架）

## 📦 打包

```bash
# 打包 Windows 安装版
npm run build:win

# 打包便携版
npm run build:portable
```

打包后的文件在 `dist` 目录。

## 📁 项目结构

```
ai-fast-assistant/
├── main.js                 # Electron 主进程
├── preload.js              # 安全桥接
├── src/
│   ├── deepseek-client.js  # DeepSeek API 客户端
│   ├── window-manager.js   # 窗口管理
│   └── config-manager.js   # 配置管理（含预设模板）
├── renderer/
│   ├── index.html          # 主界面
│   ├── styles.css          # 样式
│   └── renderer.js         # 渲染进程逻辑
└── assets/
    └── icon.ico            # 应用图标
```

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

如果你有好的想法或发现了 Bug，请：
1. Fork 本仓库
2. 创建你的特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交你的修改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开一个 Pull Request

## 📝 开发计划

- [ ] 支持更多 AI 服务（OpenAI、Claude、Gemini）
- [ ] 支持导出对话记录
- [ ] 支持自定义预设模板
- [ ] 支持多窗口模式
- [ ] 支持语音输入
- [ ] macOS 和 Linux 版本

## ⚠️ 注意事项

- 需要自行申请 DeepSeek API Key
- API Key 加密存储在本地，不会上传到任何服务器
- 首次使用需要联网下载依赖（marked.js、highlight.js）

## 📄 许可证

MIT License - 详见 [LICENSE](LICENSE) 文件

## 🙏 致谢

- [DeepSeek](https://www.deepseek.com/) - 提供高性价比的 AI API
- [Electron](https://www.electronjs.org/) - 跨平台桌面应用框架
- [marked.js](https://marked.js.org/) - Markdown 解析器
- [highlight.js](https://highlightjs.org/) - 代码语法高亮

---

如果这个项目对你有帮助，欢迎 ⭐ Star 支持一下！

